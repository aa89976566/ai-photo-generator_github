# 📦 下載源代碼指南

## 🎯 方法 1：使用打包腳本（推薦）

### 步驟 1：執行打包腳本

在終端機執行：

```bash
cd /Users/ming/Downloads/code_sandbox_light_299d873b_1766542518
./create-package.sh
```

這會創建兩個壓縮包：
- `haodada-ai-generator-source-YYYYMMDD-HHMMSS.zip`
- `haodada-ai-generator-source-YYYYMMDD-HHMMSS.tar.gz`

### 步驟 2：找到壓縮包

壓縮包會保存在：
```
/Users/ming/Downloads/code_sandbox_light_299d873b_1766542518/
```

---

## 🎯 方法 2：手動打包（如果腳本不工作）

### 步驟 1：在終端機執行

```bash
cd /Users/ming/Downloads/code_sandbox_light_299d873b_1766542518

# 創建 ZIP 壓縮包
zip -r haodada-ai-generator-source.zip . \
  -x "node_modules/*" \
  -x ".env" \
  -x ".env.local" \
  -x ".git/*" \
  -x ".DS_Store" \
  -x "*.log" \
  -x ".vercel/*" \
  -x "haodada-ai-generator-source*"
```

### 步驟 2：找到壓縮包

壓縮包會保存在當前目錄：
```
/Users/ming/Downloads/code_sandbox_light_299d873b_1766542518/haodada-ai-generator-source.zip
```

---

## 🎯 方法 3：直接複製整個資料夾

### 步驟 1：打開 Finder

1. 打開 Finder
2. 前往：`/Users/ming/Downloads/code_sandbox_light_299d873b_1766542518`

### 步驟 2：選擇文件

選擇以下文件夾和文件：
- ✅ `public/` 資料夾
- ✅ `api/` 資料夾
- ✅ `server.js`
- ✅ `package.json`
- ✅ `package-lock.json`
- ✅ `.gitignore`
- ✅ 所有 `.md` 文檔文件
- ✅ `vercel.json`

**不要選擇**：
- ❌ `node_modules/` 資料夾
- ❌ `.env` 文件
- ❌ `.env.local` 文件

### 步驟 3：壓縮

1. 選中所有文件
2. 右鍵點擊 → **壓縮 X 個項目**
3. 會創建 `archive.zip`

---

## 📍 壓縮包位置

所有方法創建的壓縮包都會在：

```
/Users/ming/Downloads/code_sandbox_light_299d873b_1766542518/
```

---

## 📋 壓縮包包含的文件

### ✅ 會包含：
- 所有源代碼文件（`.js`, `.html`, `.css`）
- 配置文件（`package.json`, `.gitignore`）
- 所有文檔（`.md` 文件）
- `public/` 目錄下的所有文件
- `api/` 目錄下的文件

### ❌ 不會包含：
- `node_modules/` - 依賴套件（需要重新安裝）
- `.env` - 環境變數（需要重新創建）
- `.git/` - Git 歷史記錄
- 系統文件（`.DS_Store`）

---

## 🚀 使用壓縮包

### 解壓後需要做的事：

1. **解壓縮**到新位置
2. **安裝依賴**：
   ```bash
   npm install
   ```
3. **創建 `.env` 文件**：
   ```env
   REPLICATE_API_TOKEN=r8_8BwGjrCru8vNQex891LPHf6EbRVaBAx3ezu7V
   NODE_ENV=development
   PORT=3000
   ```
4. **啟動伺服器**：
   ```bash
   npm start
   ```

---

## 💡 快速命令

**一鍵打包（複製貼上）**：

```bash
cd /Users/ming/Downloads/code_sandbox_light_299d873b_1766542518 && \
zip -r haodada-ai-generator-source.zip . \
  -x "node_modules/*" \
  -x ".env*" \
  -x ".git/*" \
  -x ".DS_Store" \
  -x "*.log" \
  -x ".vercel/*" && \
echo "✅ 壓縮包已創建：$(pwd)/haodada-ai-generator-source.zip"
```

---

## 📍 找到壓縮包

壓縮包位置：
```
/Users/ming/Downloads/code_sandbox_light_299d873b_1766542518/haodada-ai-generator-source.zip
```

或使用 Finder：
1. 打開 Finder
2. 按 `Cmd + Shift + G`
3. 輸入：`/Users/ming/Downloads/code_sandbox_light_299d873b_1766542518`
4. 找到 `haodada-ai-generator-source.zip`

---

## 🎉 完成！

現在您已經有完整的源代碼壓縮包了！

**下一步**：
- 上傳到 GitHub（參考 `UPLOAD_TO_GITHUB.md`）
- 或分享給其他人
- 或備份到雲端

