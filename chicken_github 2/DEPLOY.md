# 🚀 一鍵部署指南

## 方法 1：Railway 部署（推薦，最簡單）

### 步驟 1：上傳到 GitHub

```bash
# 在您的電腦上打開終端機/命令提示字元

# 進入專案目錄
cd chicken-ai-generator

# 初始化 Git
git init

# 添加所有檔案
git add .

# 提交
git commit -m "Initial commit: 進擊的手撕雞 AI 生成器"

# 添加遠端倉庫（替換成您的 GitHub 用戶名）
git remote add origin https://github.com/你的用戶名/chicken-ai-generator.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步驟 2：部署到 Railway

1. 前往 https://railway.app
2. 點擊 "Login" → 用 GitHub 登入
3. 點擊 "New Project"
4. 選擇 "Deploy from GitHub repo"
5. 選擇 `chicken-ai-generator`
6. Railway 會自動：
   - 偵測 Python 專案
   - 安裝 requirements.txt
   - 執行 Procfile
   - 生成公開網址

### 步驟 3：獲取網址

1. 部署完成後（約 3-5 分鐘）
2. 點擊 "Settings" → "Domains"
3. 點擊 "Generate Domain"
4. 複製網址（例如：`your-app.up.railway.app`）

### 步驟 4：測試

1. 開啟網址
2. 點擊「加入 LINE 好友」
3. 測試上傳照片
4. 確認生成功能正常

---

## 方法 2：Render 部署（免費方案）

### 步驟 1：上傳到 GitHub（同上）

### 步驟 2：部署到 Render

1. 前往 https://render.com
2. 用 GitHub 登入
3. 點擊 "New +" → "Web Service"
4. 選擇您的 GitHub repository
5. 設定：
   - Name: `chicken-ai-generator`
   - Environment: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app`
6. 點擊 "Create Web Service"

### 步驟 3：等待部署（約 5-10 分鐘）

---

## 方法 3：本地測試

```bash
# 安裝依賴
pip install -r requirements.txt

# 啟動服務
python app.py

# 瀏覽器開啟
http://localhost:5000
```

---

## 🎯 部署後檢查清單

- [ ] 網站可以正常開啟
- [ ] 點擊「加入 LINE 好友」會跳轉到 @902rkfzv
- [ ] 可以上傳照片
- [ ] 可以選擇風格
- [ ] 點擊生成會顯示結果（測試版顯示範例照片）
- [ ] 分享按鈕正常運作

---

## ⚠️ 常見問題

### Q: Railway 部署失敗？
A: 檢查 `requirements.txt` 和 `Procfile` 是否正確

### Q: 網站顯示 502 錯誤？
A: 等待 3-5 分鐘，Railway 正在啟動服務

### Q: 照片上傳後沒有真實生成？
A: 這是正常的！測試版顯示範例照片
   要真實生成需要啟用後端圖像處理（已包含在 app.py）

### Q: 如何啟用真實 AI 生成？
A: 已經包含！Railway 部署後會自動啟用
   本地測試時執行 `python app.py` 即可

---

## 📊 成本說明

### Railway 免費額度
- $5 美金/月
- 足夠處理 500-1000 次生成
- 適合初期測試

### Render 免費方案
- 完全免費
- 但服務會休眠（15分鐘無活動）
- 第一次訪問需等待喚醒（~30秒）

---

## 🎉 部署成功！

網站上線後：
1. 在 Threads 發布宣傳文案
2. 開始收集 LINE 好友
3. 監控使用數據

**恭喜！您的 AI 生成器已經上線！** 🚀
